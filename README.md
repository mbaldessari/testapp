- Add two templates/steps:
	1. To deploy the argocd upstream/latest operator + latest argo
	3. To deploy the argocd gitops supported operator

- Create a small helm application with one values-override.yaml file
  
